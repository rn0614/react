//1.js의 값중 특이값
//  infinity, NaN, undefined, null, globalThis(전역 this값)

// js는 최대값이 존재
const maxNum = Math.pow(10,1000);

if(maxNum===Infinity){
    console.log(`maxNum은 Infinity 입니다.`)
}


//2. 함수속성
// eval, isFinite(), isNaN()
// parseFloat(), parseInt(), 
// encodeURI(), decodeURI(), encodeURIComponent(), decodeURIComponent()


//3. 기초 객체의 종류
// Object, Function, Boolean, Symbol 
